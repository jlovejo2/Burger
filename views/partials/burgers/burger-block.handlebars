{{#unless vomit_amt}}
<li>
	{{name}}

	<button class="change-eaten" data-id="{{id}}" data-neweaten="{{eat}}" data-vomitAmt="{{vomit}}">
		{{#if eat}}EAT IT EAT IT!{{else}}I Don't feel so good!{{/if}}
	</button>

</li>
{{/unless}}


{{#if vomit_amt}}
<li class="vomit-li">

	{{name}}..........but soggy
<br>
<button class="delete-burger" data-id="{{this.id}}">eat it?? Are you sure??</button>

</li>
{{/if}}